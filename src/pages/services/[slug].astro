---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
import Footer from '../../components/Footer.astro';
import ScrollTop from '../../components/ScrollTop.astro';

export async function getStaticPaths() {
  const posts = await getCollection('servicepage');
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { data } = post;
const { Content } = await post.render();

---


<Layout title={data.seo.metaTitle} description={data.seo.metaDescription} keywords={data.seo.keywords}>
  <main role="main" class="pt-12">
    
    <!-- Hero Section -->
    <section class="sm:mt-12 lg:mt-16 mt-16 px-4 sm:px-6 lg:px-8">
      <div class="my-10 mx-auto max-w-8xl px-4 sm:mt-12 sm:px-6 md:mt-16 lg:mt-20 lg:px-8 xl:mt-28 flex gap-3 lg:flex-justify lg:flex flex-col lg:flex-row">
        
        <!-- Left Content -->
        <div class="sm:text-center lg:text-left lg:w-1/2">
          <h1 class="text-4xl tracking-tight font-extrabold text-gray-700 sm:text-5xl md:text-6xl mb-4" data-aos="fade-up" data-aos-delay="100" data-aos-duration="900">
            <span class="block xl:inline capitalize">{data.heroTitle}</span>
          </h1>
          
          <p class="leading-none text-homeblack text-gray-500 text-[24px] font-bold mt-1" data-aos="fade-up" data-aos-delay="200" data-aos-duration="900">
            {data.heroDescription}
          </p>
          
          <p class="mt-3 text-base text-gray-700 sm:mt-5 sm:text-lg sm:max-w-xl sm:mx-auto md:mt-5 md:text-xl lg:mx-0 mb-2" data-aos="fade-up" data-aos-delay="300" data-aos-duration="900">
            {data.heroParagraph}
          </p>
          
          <!-- Features List -->
          {data.features.map((feature, index) => (
            <div class="flex items-center space-x-3" data-aos="fade-up" data-aos-delay={400 + (index * 100)} data-aos-duration="900">
              <span class="from-blue-600 via-green-600 to-blue-600 bg-linear-to-r bg-clip-text text-transparent text-4xl">●</span>
              <p class="text-gray-700 text-lg">
                <strong class="font-semibold text-blue-600">{feature.title}:</strong> {feature.description}
              </p>
            </div>
          ))}
          
          <!-- Button Section -->
          <div class="mt-5 sm:mt-8 sm:flex sm:justify-center lg:justify-start" data-aos="fade-up" data-aos-delay="1100" data-aos-duration="900">
            <div class="rounded-md shadow-lg">
              <a href="/contact-us" class="bg-linear-to-r from-blue-800 to-green-800 w-full flex items-center justify-center px-8 py-3 text-base leading-6 font-medium text-white transition-all duration-200 ease-in-out md:py-4 md:px-10 hover:from-blue-700 hover:to-green-700 hover:scale-105 transform rounded">
                Get Started
              </a>
            </div>
            <div class="mt-3 sm:mt-0 sm:ml-3">
              <a href="tel:+919507925579" class="w-full flex items-center justify-center px-8 py-3 text-base leading-6 font-medium bg-transparent text-blue-700 border-2 border-blue-500 hover:bg-linear-to-r hover:from-blue-700 hover:to-green-700 hover:text-white hover:border-transparent transition-all duration-200 ease-in-out transform rounded">
                Call Now
              </a>
            </div>
          </div>
        </div>
        
        <!-- Right Side - Contact Form -->
        <div class="lg:w-1/2 w-full mt-8 lg:mt-0" data-aos="fade-up" data-aos-delay="100" data-aos-duration="900">
          <div class="bg-linear-to-r from-blue-400 to-green-400 px-4 py-4 rounded-t-lg">
            <span class="text-3xl font-semibold text-gray-900 text-center justify-centre">Get In Touch With Us</span>
          </div>
          <form id="contactForm" class="space-y-4 p-6 bg-white shadow-lg rounded-lg">
            <!-- Contact form fields here (same as your original) -->
             <input type="text" name="website_url" style="display:none !important" tabindex="-1" autocomplete="off">
            
            <div>
              <label for="name" class="block text-sm font-medium text-gray-600">Full Name *</label>
              <input type="text" id="name" name="name" placeholder="Enter Full Name" class="mt-2 block w-full p-2 border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-100" required maxlength="100">
            </div>
            
            <div>
              <label for="email" class="block text-sm font-medium text-gray-600">Email Address *</label>
              <input type="email" id="email" name="email" placeholder="Enter Email Id." class="mt-2 block w-full p-2 border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-100" required>
              <span class="text-red-500 text-sm email-error hidden"></span>
            </div>
            
            <div>
              <label for="number" class="block text-sm font-medium text-gray-600">Phone Number *</label>
              <input type="tel" id="mobile" placeholder="+91 98765 43210" maxlength="10" name="mobile" class="mt-2 block w-full p-2 border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-100" required>
              <span class="text-red-500 text-sm mobile-error hidden"></span>
            </div>
            
            <div>
              <label for="website" class="block text-sm font-medium text-gray-600">Your website (Optional)</label>
              <input type="url" id="website" name="website" class="mt-2 block w-full p-2 border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-100" placeholder="enter your website url eg:- www.example.com">
            </div>
            
            <div>
              <label for="message" class="block text-sm font-medium text-gray-600">Message *</label>
              <textarea id="message" name="message" rows="4" class="mt-2 block w-full p-2 border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-100" placeholder="Enter Message" required></textarea>
              <span class="text-red-500 text-sm message-error hidden"></span>
            </div>
            
            <div>
              <button type="submit" id="submitBtn" class="bg-linear-to-r from-blue-800 to-green-800 w-full flex items-center justify-center px-8 py-3 text-base leading-6 font-medium text-white md:py-4 md:px-10 hover:from-blue-700 hover:to-green-700 rounded">
                SUBMIT
              </button>
            </div>

            <div class="loading" id="loading">
                <div class="spinner"></div>
                <p>Sending...</p>
            </div>
            
            <div class="message" id="responseMessage"></div>
          </form>
        </div>
      </div>
    </section>
    
    <!-- Services Section -->
    <section class="py-8">
      <div class="container mx-auto px-4">
        <div class="text-center mb-6 md:mb-8">
          <div class="text-2xl font-semibold text-neutral-800 pb-4" data-aos-duration="900" data-aos="fade-up" data-aos-delay="100">
           OTHER SERVICES WE OFFER
          </div>
          <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl capitalize mb-4" data-aos-duration="900" data-aos="fade-up" data-aos-delay="200">
            
           {data.sertvicetitle}
          </h2>

          <p class="text-gray-800 text-xl mx-auto" data-aos="fade-up" data-aos-duration="900">
            {data.sertvicedisc}
          </p>

        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {data.ourservices.map((service, index) => (
            <div class="flex flex-col items-start justify-stretch py-6 px-6 min-h-72 bg-linear-to-bl hover:bg-linear-to-r from-green-200 to-blue-200 rounded-xl border shadow-[-3px_5px_8px_5px_rgba(0,0,0,0.35)] relative" data-aos-duration="900" data-aos="zoom-in" data-aos-delay={index * 100}>
              <div class="bg-linear-to-r from-blue-400 to-green-400 w-28 h-28 rotate-45 m-auto my-6 p-4 rounded-lg transition-transform duration-300 ease-in-out hover:animate-bounce">
                <div class="object-cover p-1 m-auto -rotate-45 place-content-center filter invert-26 sepia-82 saturate-7485 hue-rotate-186 brightness-106 contrast-103">
                   <img src={service.icon} alt={service.alt} class="h-16 w-16" />
                </div>
              </div>
              <div class="mt-4 text-center w-full">
                <h3 class="font-semibold text-2xl pt-4 text-neutral-800 mb-3">{service.title}</h3>
                <p class="text-gray-500 font-medium lg:text-lg sm:text-md">{service.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
    
    <!-- Why Choose Us Section -->
    <section class="py-20">
      <div class="container mx-auto px-12 py-12 bg-linear-to-r from-green-400 to-blue-400 rounded-2xl">
        <div class="text-center mb-16">
          <h2 class="hero-title text-4xl md:text-5xl font-black text-gray-900 mb-6" data-aos="fade-up" data-aos-duration="900">
            {data.whychoosetitle}
          </h2>
          <p class="text-gray-800 text-xl mx-auto" data-aos="fade-up" data-aos-duration="900">
            {data.whychoosedisc}
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
          {data.whyChooseUs.map((item, index) => (
            <div class="bg-linear-to-bl hover:bg-linear-to-r from-green-200 to-blue-200 p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300" data-aos="fade-up" data-aos-duration="900" data-aos-delay={100 + (index * 100)}>
              <div class="w-8 h-8 text-2xl font-bold bg-linear-to-r from-blue-400 to-green-400 rounded-full flex items-center justify-center mb-6">
                {item.number}
              </div>
              <h3 class="text-2xl font-bold text-gray-900 mb-4">{item.title}</h3>
              <p class="text-gray-800">{item.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>




    <section class="relative bg-linear-to-r from-blue-400 to-green-400">
	<div class="absolute top-10 right-10 w-64 h-64 bg-blue-300 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse"></div>
        <div class="absolute bottom-10 left-10 w-64 h-64 bg-green-400 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse"></div>

  <div class="container mx-auto px-4">
    <div class="py-12 lg:py-20 bg-cover">
      <div class="grid grid-cols-1 gap-5 lg:grid-cols-12 lg:gap-10 items-center">
        
        <!-- Image Section -->
        <div class="col-span-full lg:col-span-5">
          <div class="relative">
            <img src={data.resultdrivenimg} loading="lazy" alt="Digital Marketing company in india"
              class="w-full rounded-xl object-contain object-center" data-aos="zoom-in-up" data-aos-duration="900">
          </div>
        </div>
        
        <!-- Text Section -->
        <div class="col-span-full lg:col-span-6 lg:col-start-7 py-10 lg:py-0">
          <div class="flex items-start gap-3">
            <h2 class="text-3xl font-extrabold text-gray-800 sm:text-4xl capitalize" data-aos="fade-up" data-aos-duration="900" data-aos-delay="100">
             {data.resultdriventitle}
            </h2>
          </div>
          <div class="text-lg text-gray-600 mt-4" data-aos="fade-up" data-aos-duration="900" data-aos-delay="200">
            <p>{data.resultdrivenparap1}</p>
            <p>
        {data.resultdrivenparap2}
            </p>
            <p>
           {data.resultdrivenparap3}
            </p>
          </div>
          <div class="mt-10">
            <a href="/contact-us" class="bg-green-300 hover:bg-green-400 py-2 px-4 text-xs lg:text-sm font-bold uppercase tracking-wider text-black rounded" data-aos="fade-up" data-aos-duration="900" data-aos-delay="300">
              Know More
            </a>
          </div>
        </div>
        
      </div>
    </div>
  </div>
</section>


<div class="py-24 bg-linear-to-r from-blue-400 to-green-400">
	<div class="container mx-auto px-4 text-center"> 
		<h2 class="text-2xl font-bold leading-tight text-gray-800 pb-4 sm:text-4xl lg:text-3xl capitalize" data-aos="fade-up" data-aos-duration="900" data-aos-delay="100">
		{data.industrytitle}
		</h2>
		<p data-aos="fade-up" data-aos-duration="900" data-aos-delay="200">
{data.industrydisc}
		   </p>

	</div>
	  
</div>
<div class="py-24 bg-linear-to-r from-blue-400 to-green-400">
</div>


 <!-- Industries Section -->
<section class="-mt-64 xsm:-mt-4 sm:mt-0 md:-mt-10 xl:-mt-64 xl1450:-mt-4 relative pb-8 sm:pb-16 overflow-visible z-10">
  <div class="container mx-auto z-10 relative">
    <div class="flex justify-center flex-wrap">
      {data.industries.map((industry, index) => (
        <div
          class="px-2 lg:px-3 mb-4 mt-3 sm:w-1/2 md:w-1/4"
          data-aos="zoom-in"
          data-aos-duration="900"
          data-aos-delay={index * 100}
          key={industry.title} 
        >
          <div class="text-gray-900 bg-linear-to-bl hover:bg-linear-to-r from-green-200 to-blue-200 text-center flex flex-col justify-start items-center mx-auto h-full transition-transform transform hover:scale-105 duration-300 ease-in-out px-3 pt-5 pb-6 sm:m-0 shadow-[-3px_5px_8px_5px_rgba(0,0,0,0.35)] hover:shadow-[-3px_5px_8px_5px_rgba(0,0,0,0.35)]">
            <img
              decoding="async"
              src={industry.img} 
              loading="lazy"
              alt={industry.title} 
              class="inline-block rounded-xl h-24 mt-3 image"
              data-aos="zoom-in-up"
              data-aos-duration="900"
            />
            <p class="font-bold mt-6 text-xs sm:text-sm lg:text-lg uppercase">
              {industry.title}
            </p>
            <span class="text-secondary-color double-caret"></span>

            <div class="space-y-4 mt-4">
              {industry.categories.map((category) => (
                <div class="flex items-center justify-start" key={category}>
                    <Icon name="mdi:check-all" class="mr-4 lg:mr-6 text-gray-800 text-2xl" />
                  <span class="font-medium text-base text-paragraph-default transition-all duration-300 ease-in-out hover:text-gray-700">
                    {category}
                  </span>
                </div>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>



    <section class="py-10 sm:py-16 lg:py-14">
  <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
    <div class="max-w-2xl mx-auto text-center">
      <h2
        class="text-2xl font-bold leading-tight text-gray-800 sm:text-4xl lg:text-3xl capitalize"
        data-aos="fade-up"
        data-aos-duration="900"
        data-aos-delay="100"
      >
      {data.faqmaintitle}
      </h2>
    </div>

    <div class="mx-auto mt-8 space-y-4 md:mt-16">
      {data.faqs.map((faq, index) => (
        <div
          class="transition-all duration-200 bg-white border border-gray-200 shadow-lg cursor-pointer bg-linear-to-bl hover:bg-linear-to-r from-green-200 to-blue-200"
          data-aos="fade-up"
          data-aos-duration="900"
          data-aos-delay={100 + index * 100}
          key={index}
        >
          <button
            type="button"
            id={`question${index}`}
            data-state="closed"
            class="flex items-center justify-between w-full px-4 py-5 sm:p-6"
          >
            <span class="flex text-lg font-semibold text-gray-800">{faq.question}</span>
            <Icon
              name="line-md:arrow-down-circle"
              class="h-12 w-12 text-gray-700 transition-transform transform rotate-0 faq-icon"
              id={`arrow${index}`}
            />
          </button>

          <div
            id={`answer${index}`}
            style="display:none"
            class="px-4 pb-5 sm:px-6 sm:pb-6 text-gray-800"
          >
            <p>{faq.answer}</p>
          </div>
        </div>
      ))}
    </div>
  </div>

  <script is:inline>
    // Toggle FAQ answers and rotate arrows
    document.querySelectorAll('[id^="question"]').forEach((button, index) => {
      button.addEventListener('click', () => {
        const answer = document.getElementById(`answer${index}`);
        const arrow = document.getElementById(`arrow${index}`);
        if (answer.style.display === 'none' || answer.style.display === '') {
          answer.style.display = 'block';
          arrow.style.transform = 'rotate(0deg)';
        } else {
          answer.style.display = 'none';
          arrow.style.transform = 'rotate(-180deg)';
        }
      });
    });
  </script>
</section>


     <!-- Final CTA -->
    <section class="py-20 bg-linear-to-r from-blue-600 to-green-500">
      <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto text-center text-white">
          <h2 class="text-3xl md:text-4xl font-bold mb-6">
            {data.callToAction.title}
          </h2>
          <p class="text-xl opacity-90 mb-10">
            {data.callToAction.description}
          </p>
          <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
            <a href="/contact-us" class="group bg-white text-blue-600 font-bold px-10 py-4 rounded-xl shadow-2xl hover:shadow-3xl transform hover:-translate-y-2 transition-all duration-300 flex items-center">
              Start Your Project
              <svg class="w-5 h-5 ml-2 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
              </svg>
            </a>
            <a href="tel:+919507925579" class="group bg-transparent border-2 border-white text-white font-bold px-10 py-4 rounded-xl hover:bg-white/10 transform hover:-translate-y-2 transition-all duration-300 flex items-center">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
              </svg>
              Schedule a Call
            </a>
          </div>
          <p class="text-sm opacity-75 mt-8">
            ⚡ Get response within 30 minutes | 📞 24/7 Support Available
          </p>
        </div>
      </div>
    </section>
    

    <section class="py-12">
      <div class="container mx-auto px-4">
        <div class="prose prose-lg max-w-none">
          <Content />
        </div>
      </div>
    </section>
    
  </main>

<ScrollTop />
 <Footer />
 
  <script is:inline>
    // REPLACE WITH YOUR GOOGLE APPS SCRIPT URL
    const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbxGuKeNC-KwW5n7J-UA_1kxwgLZKY_JRnJu7idNr2OpK_dhlqqwRZlBVS3-oB8uu-GlXw/exec';
    
    const form = document.getElementById('contactForm');
    const submitBtn = document.getElementById('submitBtn');
    const loading = document.getElementById('loading');
    const responseMessage = document.getElementById('responseMessage');
    
    // Validation functions
    const validators = {
    name: (value) => {
        if (!value || value.length < 2) return 'Name must be at least 2 characters';
        if (value.length > 100) return 'Name too long';
        if (!/^[a-zA-Z\s\-'\.]+$/.test(value)) return 'Name contains invalid characters';
        return null;
    },
    
    email: (value) => {
        if (!value) return 'Email is required';
        if (!/^\S+@\S+\.\S+$/.test(value)) return 'Invalid email format';
        return null;
    },
    
    mobile: (value) => {
        if (!value) return 'Mobile number is required';
        if (!/^[0-9+\-\s()]{10,15}$/.test(value)) return 'Invalid mobile number';
        return null;
    },
    
    message: (value) => {
        if (!value) return 'Message is required';
        if (value.length < 10) return 'Message must be at least 10 characters';
        return null;
    },
    
};
    
    form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        // Hide previous messages
        responseMessage.className = 'message';
        responseMessage.style.display = 'none';
        
        // Honeypot check
        const honeypot = form.querySelector('input[name="website_url"]');
        if (honeypot.value) {
            showMessage('success', 'Message sent successfully!');
            form.reset();
            return;
        }
        
        // Get form data
        const formData = {
            name: form.name.value.trim(),
            email: form.email.value.trim(),
            mobile: form.mobile.value.trim(),
            website: form.website.value.trim() || 'Not provided',
            message: form.message.value.trim()
        };
        
        // Validate all fields
        for (const [field, value] of Object.entries(formData)) {
            if (validators[field]) {
                const error = validators[field](value);
                if (error) {
                    showMessage('error', error);
                    document.getElementById(field).focus();
                    return;
                }
            }
        }
        
        // Disable form and show loading
        submitBtn.disabled = true;
        loading.classList.add('show');
        
        try {
            // Create URL-encoded form data (THIS IS THE FIX)
            const urlEncodedData = new URLSearchParams();
            urlEncodedData.append('name', formData.name);
            urlEncodedData.append('email', formData.email);
            urlEncodedData.append('mobile', formData.mobile);
            urlEncodedData.append('website', formData.website);
            urlEncodedData.append('message', formData.message);
            
            // Send to Google Sheets using form-urlencoded
            const response = await fetch(GOOGLE_SCRIPT_URL, {
                method: 'POST',
                mode: 'no-cors',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: urlEncodedData.toString()
            });
            
            // With no-cors, we can't read response, assume success
            showMessage('success', 'Thank you! Your message has been sent successfully.');
            form.reset();
            
        } catch (error) {
            console.error('Error:', error);
            showMessage('error', 'Sorry, there was an error. Please try again.');
        } finally {
            submitBtn.disabled = false;
            loading.classList.remove('show');
        }
    });
    
    function showMessage(type, text) {
        responseMessage.className = `message ${type}`;
        responseMessage.textContent = text;
        responseMessage.style.display = 'block';
        
        if (type === 'success') {
            setTimeout(() => {
                responseMessage.style.display = 'none';
            }, 5000);
        }
    }
    
    // Real-time validation
    ['name', 'email', 'mobile', 'message', 'website'].forEach(field => {
        const element = document.getElementById(field);
        if (element) {
            element.addEventListener('blur', function() {
                const error = validators[field](this.value.trim());
                this.style.borderColor = error ? '#dc3545' : '#28a745';
            });
            
            element.addEventListener('input', function() {
                this.style.borderColor = '#ddd';
            });
        }
    });
    </script>
</Layout>